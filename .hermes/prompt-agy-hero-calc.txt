/goal Dos tareas: (1) Hero con slider sticky y scroll-through del texto, (2) Calculadora mejorada estilo RealThread. Trabaja en /mnt/c/Users/juanp/Desktop/mas-imagen-web/

PRIMER COMANDO: cd /mnt/c/Users/juanp/Desktop/mas-imagen-web

## TAREA 1: Hero "sticky slider" — el slider se queda fijo mientras el texto aparece con scroll

En src/app/page.tsx, modificar la sección del hero:

- El section hero debe tener altura h-[200vh] (doble de pantalla)
- El contenedor del slider (imágenes + overlay) debe ser sticky top-0 h-screen
- El contenido de texto (título + subtítulo + botones) ya NO usa useScroll/useTransform de framer-motion
- En su lugar, el texto está posicionado en la MITAD INFERIOR de las 200vh, de modo que:
  - Al abrir la página (scrollY=0): se ve solo el slider y "MásImagen" gigante centrado
  - Al hacer scroll (0 a 100vh): el título grande se desvanece y aparece el subtítulo + botones
  - Al llegar a 100vh de scroll: el texto ya es visible completo, el slider sigue de fondo
  - Al pasar 100vh: se sale del hero sticky y aparecen los servicios

Técnica: usa position sticky para el slider, y el contenido de texto va en la segunda mitad con position relative z-20. El efecto de fade lo hacemos con opacity basado en scroll — mantenemos useScroll pero con offset ["start start", "start center"].

## TAREA 2: Calculadora mejorada estilo RealThread

En src/components/futuro/QuoteCalculator.tsx, reescribir con estas características:

### Layout:
- Dos columnas en desktop: izquierda (2/3) controles, derecha (1/3) resumen de precios
- Fondo de la columna derecha: bg-warm-gray-100 (usa bg-[var(--muted)])

### Controles (columna izquierda):
- Quantity: pills redondeados con rangos (10-24, 25-49, 50-99, 100-249, 250-499, 500+)
- Production process: dos cards con icono (Print/Serigrafía y Embroidery/Bordado)
- Print locations: botones toggle (Frente, Atrás, Manga izq, Manga der) — múltiples selecciones
- SI es bordado: mostrar tipo de bordado (Estándar, Puff 3D)

### Sidebar de precios (columna derecha):
- Título: "Tu cotización instantánea:"
- Precio total grande con formato $X,XXX MXN
- Tabla con TODOS los tiers mostrados simultáneamente:
  - Columnas: Cantidad | Precio / pieza | Total estimado
  - La fila del tier seleccionado va resaltada (bg-muted, font-bold)
  - Las demás filas visibles pero sin resaltar
- Texto pequeño: "Pedidos 2XL+ tienen costo extra"
- Para el tier 5000+ mostrar "Contáctanos para precio"

### Lógica de precios:
- Usar matriz PRICING_MATRIX con tiered pricing
- Fórmula: basePrice + (extraLocationCost * (ubicaciones - 1))
- El total se calcula con el MÍNIMO del rango (ej: para 50-99, usa 50)

### Efecto blur al cambiar:
- Cuando el usuario cambia una opción, la columna de precios hace un blur-md breve (300ms) y luego se aclara
- Usa useState + setTimeout para el efecto

## VERIFICACIÓN
npm run build DEBE compilar sin errores.

CRITICO: NO guardes reportes. NO uses /artifact. Devuelve resumen en stdout.
